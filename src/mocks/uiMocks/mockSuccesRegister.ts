import { UiState } from "../../types/types";

const mockSuccessRegister: UiState = {
  feedback: {
    isError: false,
    isOpen: true,
    messageFeedback: "¡Bienvenido a Be Cooked!",
  },
  isLoading: false,
};

export default mockSuccessRegister;
